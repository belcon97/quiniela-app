import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {
    flex: 1,
    backgroundColor: "#fff",
  },

  content__web: {
    height: 0,
    overflow: "visible",
  },

  children: {
    flex: 1,
  },
});